extends: ../reproduction.yaml
experiment:
  name: sensitivity_h4
  seed: 2025
  additional_seeds: []
training:
  epochs: 10
  optimizer: sgd
  learning_rate: 0.01
  weight_decay: 0.0001
  momentum: 0.0
  scheduler: cosine
